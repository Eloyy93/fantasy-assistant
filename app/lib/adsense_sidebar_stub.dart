import 'package:flutter/widgets.dart';

/// Implementación vacía para plataformas no-web (Android) — AdSense es
/// solo para la versión web, aquí no hay nada que mostrar.
Widget buildAdsenseSidebar() => const SizedBox.shrink();
